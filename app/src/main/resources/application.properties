# Day 2: keep JPA and MySQL dependencies while running without a database.
# Remove this exclusion when the DataSource is configured on Day 3.
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
